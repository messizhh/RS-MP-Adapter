class PrototypeBuilder:
    """Reserved prototype builder interface."""

    def build(self, *args, **kwargs):
        raise NotImplementedError("Prototype construction is out of scope for Phase 1A.")
