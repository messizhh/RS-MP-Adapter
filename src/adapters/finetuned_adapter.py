class FinetunedAdapter:
    """Reserved fine-tuned adapter interface."""

    def fit(self, *args, **kwargs):
        raise NotImplementedError("Fine-tuned adapter implementation is out of scope for Phase 1A.")
