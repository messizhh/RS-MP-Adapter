# Experiment Protocol

Phase 1A only defines reproducibility infrastructure. Local runs are validation runs and are not paper-facing results.

Paper-facing tables must be generated only from explicitly approved server runs.
